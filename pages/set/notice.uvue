<template>
  <cl-page>
    <view class="p-3">
      <cl-list>
        <cl-list-item :label="t('开启通知')">
          <cl-switch v-model="notify_enable" @change="onNotifyChange"></cl-switch>
        </cl-list-item>
      </cl-list>
    </view>
  </cl-page>
</template>

<script setup lang="ts">
import {t} from "@/locale";
import {changeNotify, notify_enable} from "@/components/msg-notifier";


function onNotifyChange(val: boolean) {
  changeNotify(val);
}
</script>
