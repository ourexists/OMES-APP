import { RequestOptions } from "../core/service/index";
import { config, isDev } from "@/config";
import { apiPath } from "@/core/apiRouter/path";
import { userInfo } from "@/core/store";
import { ref } from "vue";
import { storage } from "@/uni_modules/cool-unix";
import { request } from "@/core/service";
import { parseData } from "@/core/utils/parse";
import type { Message } from "@/core/types";
let isStop: boolean = false;
let isConnected = false;
let isConnecting = false;
export const notifyQueue = ref<Message[]>([]);
export const notify_message = ref<string>("");
export const notify_visible = ref<boolean>(false);
export const notify_enable = ref<boolean>(true);
export const unread_count = ref<number>(0);
export function initNotifyEnable() {
    const saved = storage.get("notify_enable");
    if (saved == null) {
        notify_enable.value = true;
    }
    else if (typeof saved === "boolean") {
        notify_enable.value = saved as boolean;
    }
    else if (typeof saved === "number") {
        notify_enable.value = saved as number !== 0;
    }
    else if (typeof saved === "string") {
        notify_enable.value = saved as string === "true" || saved as string === "1";
    }
    else {
        notify_enable.value = true; // 兜底
    }
}
initNotifyEnable();
export async function connectMessage(): Promise<void> {
    __f__('log', 'at components/msg-notifier.ts:44', "sse connect start");
    if (isConnected || isConnecting) {
        __f__('log', 'at components/msg-notifier.ts:46', "sse connect is connecting");
        return;
    }
    isStop = false;
    isConnecting = true;
    function poll() {
        if (isStop)
            return;
        try {
            const now = Date.now();
            request({
                url: apiPath["message_page"] as string,
                method: "POST",
                data: {
                    page: 1,
                    pageSize: 10,
                    accId: userInfo.value?.id,
                    platform: config.platform,
                    createdTimeStart: formatDateTime(now - 3000),
                    createdTimeEnd: formatDateTime(now)
                }
            } as RequestOptions)
                .then((res) => {
                if (res != null) {
                    const r = parseData<Message[]>(res);
                    if (r == null) {
                        return;
                    }
                    r.forEach((msg: Message) => pushNotifyQueue(msg));
                }
            })
                .catch((err) => {
                __f__('error', 'at components/msg-notifier.ts:78', err);
            });
            request({
                url: apiPath["message_unread_count"] as string,
                method: "GET",
            } as RequestOptions)
                .then((res) => {
                if (res != null) {
                    unread_count.value = res as number;
                }
            })
                .catch((err) => {
                __f__('error', 'at components/msg-notifier.ts:91', err);
            });
        }
        catch (err: any) {
            __f__('warn', 'at components/msg-notifier.ts:94', "poll error", err);
        }
        finally {
            setTimeout(() => {
                poll();
            }, 3000); // 3秒轮询一次
        }
    }
    poll();
}
export function formatDateTime(ts: number): string {
    const d = new Date(ts);
    const p = (n: number): string => n < 10 ? '0' + n : '' + n;
    return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())} ${p(d.getHours())}:${p(d.getMinutes())}:${p(d.getSeconds())}`;
}
export function disconnectMessage() {
    __f__('log', 'at components/msg-notifier.ts:113', "sse client disconnect=======");
    isStop = true;
    isConnected = false;
    isConnecting = false;
}
export function pushNotifyQueue(msg: Message) {
    notifyQueue.value.push(msg);
    //保持数据容量不超过10条,防止内存问题
    if (notifyQueue.value.length > 10) {
        notifyQueue.value.shift();
    }
}
export function onNotify() {
    if (notifyQueue.value.length <= 0) {
        notify_visible.value = false;
    }
    else {
        const msg = notifyQueue.value.shift();
        if (msg == null) {
            return;
        }
        notify_visible.value = notify_enable.value ?? true;
        if (isDev) {
            __f__('log', 'at components/msg-notifier.ts:144', JSON.stringify(msg) + "-" + notify_visible.value + "-" + notify_enable.value);
        }
        notify_message.value = msg.context;
    }
}
setInterval(() => {
    onNotify();
}, 8000);
function clear() {
    notifyQueue.value = [];
    notify_message.value = '';
    notify_visible.value = false;
}
export function changeNotify(val: boolean) {
    storage.set("notify_enable", val, 0);
    notify_enable.value = val;
}
//# sourceMappingURL=msg-notifier.uts.map